# TODO

- [ ] Inspect current CTA button styling in `src/components/Navbar.vue`.
- [ ] Create an improved “Book Appointment” design matching the requested indigo/purple/pink gradient style.
- [ ] Update the router-link CTA classnames (and optionally add SVG icon) while keeping layout consistent.
- [ ] Verify Tailwind classes don’t break lint/build.
- [ ] (If needed) run a quick dev/build check.

